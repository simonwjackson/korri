diff --git a/command.c b/command.c
index 791d510..0969180 100644
--- a/command.c
+++ b/command.c
@@ -229,6 +229,20 @@ static void command_network_poll(command_t *handle)
 
       buf[ret] = '\0';
 
+#if defined(ANDROID)
+      {
+         char *line_end = strpbrk(buf, "\r\n");
+         if (line_end)
+            *line_end = '\0';
+         if (     strcmp(buf, "GET_STATUS") != 0
+               && strcmp(buf, "QUIT") != 0)
+         {
+            RARCH_WARN("[NetCMD] Rejected command outside Korri allowlist.\n");
+            continue;
+         }
+      }
+#endif
+
       command_parse_msg(handle, buf);
    }
 }
@@ -239,7 +253,13 @@ command_t* command_network_new(uint16_t port)
    command_t            *cmd = (command_t*)calloc(1, sizeof(*cmd));
    command_network_t *netcmd = (command_network_t*)calloc(
                                    1, sizeof(command_network_t));
-   int fd = socket_init((void**)&res, port, NULL,
+#if defined(ANDROID)
+   /* Korri Android command channel is loopback-only. */
+   const char *bind_host = "127.0.0.1";
+#else
+   const char *bind_host = NULL;
+#endif
+   int fd = socket_init((void**)&res, port, bind_host,
          SOCKET_TYPE_DATAGRAM, AF_INET);
 
    RARCH_LOG("[NetCMD] %s %hu.\n",
@@ -1041,9 +1061,16 @@ bool command_get_status(command_t *cmd, const char* arg)
    {
       /* add some content info */
       core_info_t *core_info      = NULL;
+      const char *system_id       = NULL;
       runloop_state_t *runloop_st = runloop_state_get_ptr();
 
       core_info_get_current_core(&core_info);
+      if (core_info)
+         system_id = core_info->system_id;
+      if (string_is_empty(system_id))
+         system_id = runloop_st->system.info.library_name;
+      if (string_is_empty(system_id))
+         system_id = "unknown";
 
       _len     = strlcpy(reply, "GET_STATUS ", sizeof(reply));
       if (runloop_st->flags & RUNLOOP_FLAG_PAUSED)
@@ -1051,12 +1078,7 @@ bool command_get_status(command_t *cmd, const char* arg)
       else
          _len += strlcpy(reply + _len, "PLAYING", sizeof(reply) - _len);
       _len    += strlcpy(reply + _len, " ", sizeof(reply) - _len);
-      if (core_info)
-         _len += strlcpy(reply + _len, core_info->system_id,
-               sizeof(reply) - _len);
-      else
-         _len += strlcpy(reply + _len, runloop_st->system.info.library_name,
-               sizeof(reply) - _len);
+      _len    += strlcpy(reply + _len, system_id, sizeof(reply) - _len);
       _len    += strlcpy(reply + _len, ",", sizeof(reply) - _len);
       _len    += strlcpy(reply + _len,
             path_basename(path_get(RARCH_PATH_BASENAME)), sizeof(reply) - _len);
diff --git a/config.def.h b/config.def.h
index 4ffbb01..b96b9d8 100644
--- a/config.def.h
+++ b/config.def.h
@@ -1480,7 +1480,11 @@
 #define DEFAULT_RUN_AHEAD_HIDE_WARNINGS false
 
 /* Enable stdin/network command interface. */
+#if defined(ANDROID)
+#define DEFAULT_NETWORK_CMD_ENABLE true
+#else
 #define DEFAULT_NETWORK_CMD_ENABLE false
+#endif
 #define DEFAULT_NETWORK_CMD_PORT 55355
 #define DEFAULT_NETWORK_REMOTE_BASE_PORT 55400
 #define DEFAULT_STDIN_CMD_ENABLE false
diff --git a/pkg/android/phoenix-common/jni/Android.mk b/pkg/android/phoenix-common/jni/Android.mk
index 8d9c779..c6554fc 100644
--- a/pkg/android/phoenix-common/jni/Android.mk
+++ b/pkg/android/phoenix-common/jni/Android.mk
@@ -109,6 +109,7 @@ DEFINES += -DRARCH_MOBILE \
 	   -DHAVE_NETWORKGAMEPAD \
 	   -DHAVE_NETWORKING \
 	   -DHAVE_NETWORK_CMD \
+	   -DHAVE_COMMAND \
 	   -DHAVE_CLOUDSYNC \
 	   -DHAVE_IFINFO \
 	   -DHAVE_NETPLAYDISCOVERY \
