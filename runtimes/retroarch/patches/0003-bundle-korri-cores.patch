diff --git a/pkg/android/phoenix/src/com/retroarch/browser/retroactivity/RetroActivityFuture.java b/pkg/android/phoenix/src/com/retroarch/browser/retroactivity/RetroActivityFuture.java
index f74c9d1..e03cb9b 100644
--- a/pkg/android/phoenix/src/com/retroarch/browser/retroactivity/RetroActivityFuture.java
+++ b/pkg/android/phoenix/src/com/retroarch/browser/retroactivity/RetroActivityFuture.java
@@ -16,0 +17,4 @@ import com.retroarch.browser.preferences.util.UserPreferences;
+import java.io.File;
+import java.io.FileOutputStream;
+import java.io.IOException;
+import java.io.InputStream;
@@ -57,0 +62,25 @@ public final class RetroActivityFuture extends RetroActivityCamera {
+  private void installBundledCore() {
+    File coreDir = new File(getApplicationInfo().dataDir, "cores");
+    File target = new File(coreDir, "mgba_libretro_android.so");
+    File temporary = new File(coreDir, "mgba_libretro_android.so.tmp");
+    if (!coreDir.isDirectory() && !coreDir.mkdirs()) {
+      throw new IllegalStateException("Cannot create bundled core directory");
+    }
+    try (InputStream input = getAssets().open("cores/mgba_libretro_android.so");
+         FileOutputStream output = new FileOutputStream(temporary, false)) {
+      byte[] buffer = new byte[64 * 1024];
+      int count;
+      while ((count = input.read(buffer)) != -1) {
+        output.write(buffer, 0, count);
+      }
+      output.getFD().sync();
+    } catch (IOException error) {
+      throw new IllegalStateException("Cannot install bundled mGBA core", error);
+    }
+    if ((target.exists() && !target.delete()) || !temporary.renameTo(target)) {
+      throw new IllegalStateException("Cannot publish bundled mGBA core");
+    }
+    target.setReadable(true, true);
+    target.setExecutable(true, true);
+  }
+
@@ -59,0 +89 @@ public final class RetroActivityFuture extends RetroActivityCamera {
+    installBundledCore();
