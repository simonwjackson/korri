--- a/src/platform/linux/graphics.cpp
+++ b/src/platform/linux/graphics.cpp
@@ -985,6 +985,11 @@ namespace egl {
 
     gl::ctx.Flush();
 
+    // Korri live-resolution diagnostic: make GL writes to the exported VAAPI
+    // target surface complete before AVCodec/VAAPI consumes the AVFrame. The
+    // runtime-resolution encoder restart can otherwise encode stale surface data.
+    gl::ctx.Finish();
+
     return 0;
   }
 }  // namespace egl
