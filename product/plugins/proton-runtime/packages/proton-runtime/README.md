# korri-proton-runtime

Installs `share/korri/proton-runtime/setup-env`, a sourceable shell helper that configures the current Bandai Proton 10.0 direct-Wine environment: `wine64`, `WINEPREFIX`, Wine DLL paths, DXVK/VKD3D DLL copy, and OpenGL driver path.
