# korri-fex-runtime

Installs `share/korri/fex-runtime/setup-env`, a sourceable shell helper that configures the FEX rootfs, per-user FEX server socket, Vulkan/GL/drm/Wayland thunks, and x86_64 Freedreno ICD path used by Bandai-class aarch64 devices.
