# control-bridge

Nix package for the Gamescope runtime-control command-line tools:

- `gamescope-control`
- `gamescope-control-bridge`

The derivation is owned by the Gamescope plugin but is currently built from the root flake so it can receive repo-level source, Bun dependency, and tooling context.
