# Platform protocol

Framework-neutral wire contracts, schemas, RPC envelopes, and typed errors. Existing protocol modules still being migrated remain under `korri/shared` until their callers can be moved without widening this slice.
