# Capability-declared app integrations and Steam defaults

## Intent

Define a phased integration-module concept that lets built-in app integrations contribute natural baseline defaults into Korri's existing cascade, starting with Steam defaulting to the controller-safe Steam-inside-Gamescope launch shape.

## Status

Requirements captured in `requirements.md`.
