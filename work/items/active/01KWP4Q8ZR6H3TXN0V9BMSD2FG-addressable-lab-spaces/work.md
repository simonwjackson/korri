---
id: 01KWP4Q8ZR6H3TXN0V9BMSD2FG
title: Route-first addressable spaces (URL state + un-syncable lab frames)
status: active
created: 2026-07-03
source: user
---

# Route-first addressable spaces (URL state + un-syncable lab frames)

Make a Shift "space" directly addressable, where a space is `route × the state that defines the screen`. Graduate reviewable in-page view-state (Library `lens`/`sort`) from local `useState` into typed URL search params so one value is both deep-linkable in the app and readable in the lab. Let each committed route declare the state axes it needs. Evolve the DevLab so frames can be un-synced (each frame its own route) and carry a per-frame route identity, then drive an axis panel route-first: pick a route, see exactly the axes required to compose a valid, jumpable space. Built on the completed multi-device lab (`01KVXF5CGMQXZRAE27TZ3QHXRC`); full per-frame coordinate identity and shareable arrangements (Tier 3) are deferred.
