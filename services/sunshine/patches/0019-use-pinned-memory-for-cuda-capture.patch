--- a/src/platform/linux/cuda.cu
+++ b/src/platform/linux/cuda.cu
@@ -116,6 +116,16 @@
     CU_CHECK_IGNORE(cudaStreamDestroy(ptr), "Couldn't free cuda stream");
   }
 
+  std::uint8_t *alloc_pinned_ram(std::size_t size) {
+    void *data = nullptr;
+    CU_CHECK_PTR(cudaMallocHost(&data, size), "Couldn't allocate pinned CUDA capture memory");
+    return static_cast<std::uint8_t *>(data);
+  }
+
+  void free_pinned_ram(void *data) {
+    CU_CHECK_IGNORE(cudaFreeHost(data), "Couldn't free pinned CUDA capture memory");
+  }
+
   stream_t make_stream(int flags) {
     cudaStream_t stream;
 
--- a/src/platform/linux/cuda.h
+++ b/src/platform/linux/cuda.h
@@ -6,6 +6,7 @@
 
 #if defined(SUNSHINE_BUILD_CUDA)
   // standard includes
+  #include <cstddef>
   #include <cstdint>
   #include <memory>
   #include <optional>
@@ -39,6 +40,8 @@
   std::unique_ptr<platf::avcodec_encode_device_t> make_avcodec_gl_encode_device(int width, int height, int offset_x, int offset_y);
 
   int init();
+  std::uint8_t *alloc_pinned_ram(std::size_t size);
+  void free_pinned_ram(void *data);
 }  // namespace cuda
 
 typedef struct cudaArray *cudaArray_t;
--- a/src/platform/linux/wlgrab.cpp
+++ b/src/platform/linux/wlgrab.cpp
@@ -26,9 +26,18 @@
 
   struct img_t: public platf::img_t {
     ~img_t() override {
+#ifdef SUNSHINE_BUILD_CUDA
+      if (cuda_pinned) {
+        cuda::free_pinned_ram(data);
+        data = nullptr;
+        return;
+      }
+#endif
       delete[] data;
       data = nullptr;
     }
+
+    bool cuda_pinned {false};
   };
 
   class wlr_t: public platf::display_t {
@@ -322,7 +331,16 @@
       img->height = height;
       img->pixel_pitch = 4;
       img->row_pitch = img->pixel_pitch * width;
-      img->data = new std::uint8_t[height * img->row_pitch];
+      auto size = (std::size_t) height * img->row_pitch;
+#ifdef SUNSHINE_BUILD_CUDA
+      if (mem_type == platf::mem_type_e::cuda) {
+        img->data = cuda::alloc_pinned_ram(size);
+        img->cuda_pinned = img->data != nullptr;
+      }
+#endif
+      if (!img->data) {
+        img->data = new std::uint8_t[size];
+      }
 
       return img;
     }
