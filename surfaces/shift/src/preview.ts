/**
 * Tool-neutral preview composition for hosts that render Shift without Korri.
 * The fixture implements the real surface treaty entirely in memory.
 */
export {
  createFixtureHost,
  fixtureModel,
  type FixtureHost,
} from "./fixtures/fixture-host"
