import { PicoPixelDisc } from "./PicoPixelDisc"

export const name = "Pixel Disc"
export const note = "An 8×8 disc — the shape a border-radius would smooth away"

export default function PicoPixelDiscPart() {
  return <PicoPixelDisc />
}
