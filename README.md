# Starter App

Clean React + Tailwind + TanStack Router starter with Effect RPC over Hono.

## Setup

```bash
bun install
```

## Development

```bash
just dev
```

Useful commands:

```bash
just dev-web
just dev-api
just check
just test-unit
just build
just format
just lint
just typecheck
just generate-gates
```
